#!/bin/sh
# macOS/Linux launcher. Windows users should double-click Build_and_Install.cmd.
set -eu

PROJECT_DIR="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"
exec node "$PROJECT_DIR/scripts/build-and-install.mjs"
